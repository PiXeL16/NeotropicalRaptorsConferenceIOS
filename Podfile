# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'neotropicalRaptors' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
    
  pod 'XLPagerTabStrip', '~> 4.0'
  pod 'Toucan', '~> 0.5.0'
  pod 'Timepiece'
  pod 'DynamicColor', '~> 2.3.0'

  # Pods for neotropicalRaptors

  target 'neotropicalRaptorsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'neotropicalRaptorsUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
